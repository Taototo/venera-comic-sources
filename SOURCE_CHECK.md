# 漫画与视频源连通性检测

检测日期：2026-09-20。结果只代表本次网络出口；站点域名、接口和图片/CDN 可能随时变化。只有列表、详情、章节和播放地址都能确认的源才加入清单。

## 当前清单

| 显示标识 | 源 | 检测结果 |
| --- | --- | --- |
| Ven | 再漫画 | 更新列表和分类接口返回 200 |
| Ven | 漫小肆 | `jjmhw1.top`、`jjmh.top`、`jjmh.cc`、`wzd1.cc`、`wzdhm1.cc` 返回 200；`mxshm.top`、`ikanwzd.cc` 作为备用选项保留 |
| Ven | 漫画人 | 首页、搜索和正文接口返回 200 |
| Ven | 禁漫天堂 | 动态线路、列表、详情、章节和图片均验证通过 |
| Ven | 拷贝漫画 | `api.copy-manga.com` 列表、搜索、详情和章节接口返回 200；图片 CDN 可返回 200 |
| 私人 | 包子漫画 | 繁体站点入口可访问，保留简繁和 CDN 设置 |
| 私人 | 紳士漫畫 | 原默认域名不稳定，保留自定义域名和域名刷新功能 |
| 私人 | 魅影画廊 | 搜索、分类、详情和图集原图返回 200 |
| 私人 | 大人漫画 | `drmh3.com` 首页、搜索、详情、章节和图片链路返回 200；备用入口也已验证 |
| 私人 | HLZY资源 | JSON 列表、分类、详情、多线路选集和 m3u8/mp4 地址已接入 |
| 私人 | 91短视频 | `cn8.91short.com` 当前多次返回 502，保留旧适配以便站点恢复 |
| 私人 | 黄果漫剧 | `kvl1.zlgncitla.cc` 首页、详情、选集和 `videoInitialData` 中的 m3u8 地址已解析；最新地址获取页为 `huangguoai.ai` |
| 私人 | 51吃瓜 | `analyst.gcmzmnli.cc` 文章列表、分类、详情和 dplayer 标准/H265 m3u8 地址已解析 |
| 私人 | 91porna | `abf79e.qbxscriyf.cc` 首页、分类、搜索和详情可解析；适配器会继续请求 `/index/embed_play.js`，从动态响应中提取真实 m3u8 |

## 未加入清单

巴卡漫画（403）、漫画家（连接重置）、Buondua 图集（持续超时）、鸟鸟韩漫·直连（超时）、武芊漫画·直连（超时）、野蛮漫画（跳转百度）、4KHD 美图（入口不稳定）、CosplayTele（超时）、神奇漫画两站（正文规则为加密载荷）、肉漫屋（超时）、91 短视频（TLS 连接被站点主动关闭）、爱奇艺影视资源（TLS 连接超时）。

## 说明

视频清单保留 HLZY，并维护 91 短视频、黄果漫剧、51 吃瓜和 91porna；App 端统一使用通用视频详情、线路、选集和播放器模块。91porna 只有在动态接口返回真实 m3u8/mp4 后才生成 `venera-video:` 链接。

拷贝漫画的网络发现接口当前返回 `t66y.com`，该域名在本次网络中不可达；本仓库固定使用已验证的 `api.copy-manga.com`，仍可在源设置中手动更换 API 地址。
