# 漫画源连通性检测

检测日期：2026-09-20。结果只代表本次网络出口；站点域名、接口和图片/CDN 可能随时变化。可访问源只有在列表、详情、章节和播放地址都能确认时才加入清单。

## 当前清单

| 显示标识 | 源 | 检测结果 |
| --- | --- | --- |
| Ven | 再漫画 | 更新列表和分类接口返回 200 |
| Ven | 漫小肆 | `jjmhw1.top`、`jjmh.top`、`jjmh.cc`、`wzd1.cc`、`wzdhm1.cc` 返回 200；`mxshm.top`、`ikanwzd.cc` 作为备用选项补回 |
| Ven | 漫画人 | 首页、搜索和正文接口返回 200 |
| Ven | 禁漫天堂 | 动态线路 `cdnhjk.net`、`cdngwc.cc`、`cdngwc.net`、`cdngwc.club` 返回 200；列表、详情、章节和图片均验证通过 |
| Ven | 拷贝漫画 | `api.copy-manga.com` 列表、搜索、详情和章节接口返回 200；图片 CDN 可返回 200 |
| 私人 | 包子漫画 | 繁体站点入口可访问，保留简繁和 CDN 设置 |
| 私人 | 紳士漫畫 | 原默认域名不稳定，保留自定义域名和域名刷新功能 |
| 私人 | 魅影画廊 | 搜索、分类、详情和图集原图返回 200 |
| 私人 | 大人漫画 | `drmh3.com` 首页、搜索、详情、章节和图片链路返回 200；`drmh8.org`、`drmh6.xyz`、`drmh6.org`、`drmh3.org`、`drmh5.com`、`drmh2.com` 备用入口首页返回 200 |
| 私人 | ZIP0影视 | 公共搜索 API、播放详情页和 HLS 地址返回 200，已按视频标记接入 |
| 私人 | 4K厂长影视 | 首页、详情页、播放页和示例 HLS 地址返回 200；搜索页受站点 WAF 影响，需按实际网络选择域名 |
| 私人 | 牛马影院 | 首页、分类、搜索、详情、播放页和示例 m3u8 返回 200；播放页脚本内含直接地址 |
| 私人 | 天涯资源 | JSON 接口列表、分类、详情和多集 m3u8 返回 200；接口明确不支持搜索，XML 接口未作为默认实现 |
| 私人 | 巨量资源 | `api.juliang.live` JSON 接口列表、分类、搜索、详情和示例 m3u8 返回 200；大整数视频 ID 已按字符串处理 |

## 未加入清单

| 源 | 结果 | 原因 |
| --- | --- | --- |
| 巴卡漫画 | 403 | 当前入口被拒绝 |
| 漫画家 | 连接重置 | 无法稳定建立连接 |
| Buondua图集 | 本次持续超时 | 早期曾短暂可访问，复检不稳定 |
| 鸟鸟韩漫·直连 | 超时 | 列表入口不可达 |
| 武芊漫画·直连 | 超时 | API 入口不可达 |
| 野蛮漫画 | 跳转百度 | 原域名不再提供漫画页面 |
| 4KHD 美图 | 原域名跳转脚本；备用站不稳定 | `hecoq.uuss.uk` 偶尔可达，无法确认稳定链路 |
| CosplayTele | 超时 | 当前网络无法访问 |
| 神奇漫画（`sqmh.app`） | 首页和搜索 200 | 正文与章节规则是加密载荷，无法可靠迁移 |
| 神奇漫画（`shenqimanhua.net`） | 首页和搜索 200 | 正文与章节规则是加密载荷，无法可靠迁移 |
| 肉漫屋 | 超时 | 当前入口不可达 |
| 91短视频（`cn8.91short.com`） | TLS 连接被站点主动关闭 | 当前网络无法稳定访问，因此已加入可手动换域名的私人源，但未宣称已验证可用 |

拷贝漫画的网络发现接口当前返回 `t66y.com`，该域名在本次网络中不可达；本仓库固定使用已验证的 `api.copy-manga.com`，仍可在源设置中手动更换 API 地址。

## 按请求移除

GoDa漫画、comick、漫画1234、Lanraragi、Komga 和 Ven 版包子仍按此前请求移除；18真人图集、中国人能飞、歪歪漫画、直连韩漫网及私人版漫画1234也已移除。禁漫天堂和拷贝漫画已按本次请求重新加入 `index.json`。
